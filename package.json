{
  "name": "t",
  "version": "1.0.0",
  "description": "Automated tests subscription functionality",
  "scripts": {
    "install": "webdriver-manager update",
    "clean": "rimraf dist/*",
    "lint": "tslint -c tslint.json -p tsconfig.json --fix",
    "compile": "tsc",
    "protractor": "protractor dist/conf.js",
    "start": "npm install && npm-run-all clean lint compile protractor clean"
  },
  "author": "I.P.",
  "dependencies": {
    "@types/cheerio": "^0.22.15",
    "@types/jasmine": "^2.8.9",
    "@types/jasminewd2": "^2.0.6",
    "@types/node": "^12.0.10",
    "cheerio": "^1.0.0-rc.3",
    "jasmine-reporters": "^2.3.2",
    "jasmine-spec-reporter": "^4.2.1",
    "npm-run-all": "^4.1.5",
    "protractor": "^5.4.2",
    "protractor-beautiful-reporter": "^1.2.8",
    "rimraf": "^2.6.3",
    "then-request": "^6.0.2",
    "tslint": "^5.20",
    "typescript": "3.6.2"
  }
}
